# Client

React + Vite + Tailwind CSS SPA for ShramSetu.

Owns UI, routing, maps (Leaflet + OSM), and QR screens. Talks only to `server` over HTTPS/JSON. Does not access MongoDB or load XGBoost models.
