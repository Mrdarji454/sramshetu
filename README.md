# ShramSetu

Cooperative-owned digital service marketplace (SIH 2026).

Monorepo layout:

- `client/` — React + Vite + Tailwind CSS
- `server/` — Node.js + Express.js + MongoDB Atlas + JWT
- `ai-service/` — Python + FastAPI + XGBoost

See `ARCHITECTURE.md` for folder responsibilities.
